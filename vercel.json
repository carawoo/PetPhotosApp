{
  "buildCommand": "npx expo export --platform web && node inject-meta-tags.js && node rename-assets.js && cp public/sitemap.xml public/robots.txt public/favicon*.png public/icon.png dist/",
  "outputDirectory": "dist",
  "devCommand": "npx expo start --web",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!_expo|assets|favicon|icon|sitemap\\.xml|robots\\.txt|.*\\.(js|css|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot|json)).*)",
      "destination": "/index.html"
    }
  ]
}
